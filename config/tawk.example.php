<?php
/*
 * Copy this file to `config/tawk.php` and override the constants below with values
 * that match your deployment environment (e.g., disable the widget on staging).
 *
 * Make sure the file is readable by PHP but not accidentally served as text.
 */

define('TAWK_ENABLED', true);
